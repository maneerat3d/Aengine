# Self-hosted runner smoke test

Temporary PR-only file used to verify that the A-Engine Windows x64 GitHub Actions workflow is received and executed by the configured self-hosted runner.

Retest after pinning Visual Studio 2022, MSVC 14.44, Windows SDK 10.0.26100.0, and Debug/Release validation.
